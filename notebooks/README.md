# Notebooks

Place country-specific EDA notebooks here (for example, `ethiopia_eda.ipynb`, `kenya_eda.ipynb`).

Guidelines:

- Keep exploratory work in notebooks.
- Export cleaned CSV files to `data/` locally.
- Do not commit raw or cleaned CSV files.
